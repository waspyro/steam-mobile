{
  "name": "@waspyro/steam-mobile",
  "version": "1.0.3",
  "main": "dist/index.js",
  "type": "commonjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "rm -rf dist && tsc --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "description": "",
  "keywords": [],
  "author": "waspyro",
  "license": "ISC",
  "dependencies": {
    "@waspyro/steam-session": "^0.3.6",
    "steam-totp": "^2.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/steam-totp": "^2.1.2"
  }
}
